# OpenBB CLI Tools — Environment Variables for Local Testing
# Copy this file to .env and fill in your values
# .env is gitignored and will be sourced by scripts automatically

# ============================================================================
# Data Provider Configuration
# ============================================================================

# Default provider for all scripts (overrides yfinance)
# Options: yfinance (default), fmp, intrinio, tiingo, polygon, alphavantage
# Useful for Asian market tickers (.T, .TW, .HK) where yfinance is slow
OPENBB_DEFAULT_PROVIDER=yfinance

# Financial Modeling Prep API key (for enhanced earnings/ownership data)
# Get free/paid key at: https://site.financialmodelingprep.com/register
FMP_API_KEY=your_fmp_api_key_here

# Custom Python path for OpenBB (optional)
# OPENBB_PYTHON=/home/user/.local/venvs/openbb/bin/python3

# ============================================================================
# Retry/Rate Limiting Configuration
# ============================================================================

# Max retry attempts for OpenBB build lock contention
# OPENBB_RETRY_MAX_ATTEMPTS=6

# Base delay in seconds for exponential backoff
# OPENBB_RETRY_BASE_DELAY_SEC=2

# ============================================================================
# Examples for Asian Market Testing
# ============================================================================

# To test Asian market tickers with FMP instead of yfinance:
# FMP_API_KEY=your_key_here
# OPENBB_DEFAULT_PROVIDER=fmp
#
# Then run:
#   ./scripts/openbb-quote 7203.T      # Toyota (Tokyo)
#   ./scripts/openbb-ratios 2330.TW  # TSMC (Taiwan)
#   ./scripts/openbb-financials 9988.HK income 3  # Alibaba (HK)
